"""Cryptographic backends for LatCoin.

The current prototype uses a deterministic mock signature scheme so the
transaction, wallet, and chain plumbing can be exercised before a true
lattice-backed implementation is wired in.
"""
